# 第4章　漸化式から広がる数学

## 4.0 この章の地図

第2章では、漸化式をいくつかの型に分け、それぞれを計算して解いた。第3章では、問題を見たときに、差を取るのか、比を取るのか、不動点を引くのか、あるいは別の表現へ移るのかを考えた。

ここからは、これらの解法を少し遠くから眺める。

漸化式は、単に数列の項を計算するための式ではない。隣り合う項の差を調べれば、離散的な微分と積分が現れる。同じ関数を繰り返し作用させれば、反復、不動点、周期、安定性が現れる。線形な漸化式をまとめれば、行列や固有値が現れる。無限個の項を一つの式にまとめれば、母関数が現れる。そして、数列の規則をより広い変数へ延長しようとすると、特殊関数が現れる。

この章の目的は、これらの理論をすべて完成させることではない。第3章までで使った解法が、どのような数学の考え方につながっているのかを見えるようにすることである。

### 4.0.1 この章で見る四つの視点

#### 変化を見る

[
\Delta a_n=a_{n+1}-a_n
]

差分を取ると、数列がどのように変化しているかが見える。階差型漸化式を解くことは、差分を元に戻すことでもある。

#### 反復を見る

[
a_{n+1}=f(a_n)
]

という形は、関数 (f) を同じ点に何度も作用させる操作である。一般項が求まらなくても、不動点へ近づくか、周期運動になるか、発散するかを調べられる。

#### 構造を見る

線形漸化式では、数列をベクトルや行列でまとめることができる。母関数を使えば、無限個の項の関係を一つの式に移せる。

#### 表現を広げる

三角関数、階乗、積分、直交多項式などは、漸化式に現れる規則を別の形で保存したものとして見ることができる。

以降では、各節を独立した話題として暗記するのではなく、同じ漸化式を異なる言葉へ翻訳していく流れとして読む。

## 4.1 差分と離散的な微積分

### 4.1.1 数列の変化を測る

数列

[
a_0,a_1,a_2,\ldots
]

の値だけを眺めていると、規則が見えにくいことがある。そこで、隣り合う二項の差を考える。

[
\Delta a_n=a_{n+1}-a_n.
]

これを一階差分という。

例えば、

[
a_n=n^2
]

ならば、

[
\Delta a_n=(n+1)^2-n^2=2n+1
]

である。さらにもう一度差分を取ると、

[
\Delta^2a_n
=\Delta(2n+1)
=2.
]

二次式は二回の差分で定数になった。これは、二次関数を二回微分すると定数になることと似ている。

差分は、離散的な世界で変化を測る操作である。

### 4.1.2 差分を取ると次数が下がる

多項式

[
p(n)=c_mn^m+c_{m-1}n^{m-1}+\cdots+c_0
]

を考える。二項定理から、

[
(n+1)^m-n^m=mn^{m-1}+\text{それより低い次数の項}
]

である。したがって、最高次係数が0でない (m) 次多項式の差分は、(m-1) 次多項式になる。

この事実を繰り返すと、(m) 次多項式は (m) 回の差分で定数になり、(m+1) 回の差分で0になる。

数列の差分表が一定になれば、その数列が多項式で表される可能性がある。差分表は、一般項を推測するための実験道具にもなる。

### 4.1.3 階差型漸化式を和へ戻す

階差型漸化式

[
a_{n+1}-a_n=f(n)
]

を考える。両辺を (n=0,1,\ldots,N-1) について足すと、左辺は途中の項が消えて、

[
a_N-a_0=\sum_{k=0}^{N-1}f(k)
]

となる。したがって、

[
a_N=a_0+\sum_{k=0}^{N-1}f(k).
]

これは第2章で学んだ階差型の解法そのものである。ここでは、それを「差分を元に戻す離散的な積分」として見ている。

例えば、

[
a_{n+1}-a_n=2n+1,qquad a_0=0
]

ならば、

[
a_n=\sum_{k=0}^{n-1}(2k+1)=n^2
]

を得る。平方数は、最初の (n) 個の奇数を足して作ることもできる。

### 4.1.4 和の公式を設計する

逆に、

[
1^2+2^2+\cdots+n^2
]

の公式を知らないとする。この和を (S_n) とおけば、

[
S_n-S_{n-1}=n^2
]

である。

右辺が二次式なので、(S_n) は三次式になると予想する。そこで、

[
S_n=An^3+Bn^2+Cn+D
]

と置き、差分を計算する。差分が (n^2) になるように係数を決め、さらに (S_0=0) を使うと、

[
S_n=\frac{n(n+1)(2n+1)}6
]

が得られる。

重要なのは、公式を記憶していたことではない。差分を取ったときに目的の式になるものを探したことである。

## 4.2 以降の執筆メモ

- 4.2では、まず (a_{n+1}=f(a_n)) を「点の運動」として導入する。
- 4.3では、不動点を見つけるだけでは収束は証明できないことを説明する。
- 4.4では、Euler法を「微分方程式の解を一歩ずつ近似する漸化式」として扱う。
- 4.5では、Newton法を「解へ近づく数列を設計する方法」として扱う。
- 4.7では、特殊関数に入る前に母関数を説明する。
- 4.8では、チェビシェフ多項式を中心例にしてから、ガンマ関数・超幾何関数へ広げる。
